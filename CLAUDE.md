# CLAUDE.md

All project instructions are in `AGENTS.md`. Other agents (opencode) read that file too.
Change the instructions only in `AGENTS.md`, so the two files never disagree.

@AGENTS.md

## Claude Code only

- To preview the site, use the `site` server from `.claude/launch.json` (port 8000).
